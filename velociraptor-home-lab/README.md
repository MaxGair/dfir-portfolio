## Purpose

This virtual environment was built to simulate a DFIR investigation lab, consisting of a central logging server (Velociraptor) and Windows endpoints for telemetry generation and artifact collection.